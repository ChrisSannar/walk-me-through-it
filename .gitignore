# --- Vendored agent skills ---
# Materialized skills are restorable from skills-lock.json, so we don't track them.
# skills-lock.json itself IS committed (it lives at the repo root, not under these dirs).
.agents/skills/
.claude/skills/

# --- Claude Code local (personal) settings ---
.claude/settings.local.json

# --- Neovim / Lua ---
doc/tags          # generated by :helptags
*.luac            # compiled Lua
luac.out
/lua_modules/     # luarocks
.luarocks/
.tests/           # test bootstrap deps (plenary / mini.test)

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*.swo
*.swn
*~
Session.vim
.netrwhist
tags
